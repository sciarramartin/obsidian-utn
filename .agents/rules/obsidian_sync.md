# Rule: Subject-Only Knowledge Storage & Manual Git Push

Target Directory: `D:\obsidian-utn\`

1. **Subject-Only Scope:**
   Only record notes for topics, concepts, architectures, and questions related to **Integración de Aplicaciones en Entorno Web (IAEW)**. Skip all assistant meta-questions, token setup, or system diagnostics.

2. **Append-Only:**
   Append new clarifications/examples at the bottom of existing notes without deleting previous technical content.

3. **No Auto-Push:**
   Do NOT execute `git push` automatically. Keep all changes local until the user explicitly requests a push (e.g., at the end of class).
